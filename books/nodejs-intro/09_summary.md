---
title: "まとめとこれからの学び"
---

この本では以下のことを学びました。

- Node.jsとは何か
- `fnm` を使ったインストール
- `node` コマンドでのプログラム実行
- `npm` によるパッケージ管理
- `http` モジュールおよび `Express` でのWebサーバー作成
- `TypeScript` による型安全な開発
- `SQLite3` を使ったデータベース連携の基礎

## 最後に

それぞれ簡単に触れただけでしたが、これらがNode.jsをあれこれ触ってみるきっかけになれば嬉しいです。